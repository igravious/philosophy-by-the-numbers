	<div class="fields">

		<%= f.label :name %><br><!-- name of the text in the original language -->
		<%= f.text_field :name %>

		<%= f.label :name_in_english %><br><!-- name of the text in english -->
		<%= f.text_field :name_in_english %>

    <%= f.label :original_language %><br>
		<%= f.collection_select(:original_language, ::Text::ORIGINAL_LANGUAGE, :second, :first, include_blank: '…') %>

    <%= f.label :original_year %><br>
		<%= f.number_field :original_year %>

		<%= f.label :filter_id %><%= f.check_box :filter_id %>
		<span class="checkable"></span>

  </div>
