	<div id="dynamic_tag_links">
		<% unless Rails.env.test? %>
		<%= link_to_add_association 'add tag', f, :tags %>
		<% end %>
		<!-- for existing record, display each tag and the link to remove it -->
		<%= f.fields_for :tags do |tag| %>
			<%= render 'tag_fields', :f => tag %>
		<% end %>
	</div>
