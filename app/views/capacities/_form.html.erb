<%= form_for(@capacity) do |f| %>
  <% if @capacity.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@capacity.errors.count, "error") %> prohibited this capacity from being saved:</h2>

      <ul>
      <% @capacity.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :entity_id %><br>
    <%= f.number_field :entity_id %>
  </div>
  <div class="field">
    <%= f.label :label %><br>
    <%= f.text_field :label %>
  </div>
  <!-- div class="field">
    <%# f.label :relevant %><br>
    <%# f.check_box :relevant %>
	</div -->
	<div class="field">
		<label for="capacity_relevant">
			Relevant<br>
			<%= f.check_box :relevant %>
			<span class="checkable"></span>
		</label>
	</div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
