<!DOCTYPE html>
<html lang="en">
<head>
	<%= favicon_link_tag 'favicon.ico' %>
	<meta charset="utf-8">
	<meta http-equiv="X-UA-Compatible" content="IE=edge">
	<meta name="viewport" content="width=device-width, initial-scale=1">
	<!-- The above 3 meta tags *must* come first in the head; any other head content must come *after* these tags -->
	<!-- #-Phi (Philosophy by the Numbers) -->
	<title><%= @page_title ? "#{@page_title} | " : '' %>#-Phi</title>

	<!-- Fonts 
	<link rel="stylesheet" href="//fonts.googleapis.com/css?family=Roboto:300,400,500,700" type="text/css">
	<link href="//fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
	<script>
		WebFontConfig = {
			google: {
				families: ['Roboto:300,400,500,700', 'Material+Icons']
			}
		};

		(function() {
    	var wf = document.createElement('script');
			wf.src = ('https:' == document.location.protocol ? 'https' : 'http') + '://ajax.googleapis.com/ajax/libs/webfont/1/webfont.js';
			wf.type = 'text/javascript';
			wf.async = 'true';
			var s = document.getElementsByTagName('script')[0];
			s.parentNode.insertBefore(wf, s);
		})();
	</script>
	-->

	<% if Rails.env.development? %>
	<% end %>
	<%# stylesheet_link_tag    "bootstrap", media: "all" %>
	<%# stylesheet_link_tag    "bootstrap-material-design", media: "all" %>
	<%# stylesheet_link_tag    "ripples", media: "all" %>
	<%# stylesheet_link_tag    "normalize", media: "all" %>
	<%# stylesheet_link_tag    "milligram", media: "all" %>
	<!-- via sprockets bower parser to: -->
	<!-- vendor/assets/bower_components/???/bower.json -->
	<!-- if it's just the one file in main in bower.json just grab it: -->
	<!-- otherwise create a shim in vendor/assets/stylesheets/ -->
	<%= stylesheet_link_tag    "picnic", media: "all" %>
	<!-- app/assets/stylesheets/application.css -->
	<%= stylesheet_link_tag    "application", media: "all" %>
	<!-- recursively pulled in stylesheets -->

	<!-- HTML5 shim and Respond.js for IE8 support of HTML5 elements and media queries -->
	<!-- WARNING: Respond.js doesn't work if you view the page via file:// -->
	<!--[if lt IE 9]>
		<script src="https://oss.maxcdn.com/html5shiv/3.7.2/html5shiv.min.js"></script>
		<script src="https://oss.maxcdn.com/respond/1.4.2/respond.min.js"></script>
	<![endif]-->
	<!-- app/assets/javascripts/application.js -->
	<%= javascript_include_tag "application" %>
	<!-- recursively pulled in javascripts -->
	<%= csrf_meta_tags %>
</head>
<body>

<p id="notice"><%= notice %></p>

<% if flash[:notice] %>
	<div class="notice"><%= flash[:notice] %></div>
<% end %>
<% if flash[:alert] %>
	<div class="alert"><%= flash[:alert] %></div>
<% end %>

<%= yield %>

	<script type="text/javascript">
		// we're using picnic for some reason
		//$.material.init()
	</script>
</body>
</html>
