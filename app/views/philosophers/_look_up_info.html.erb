
<table>
	<thead>
		<tr>
			<th></th>
			<th></th>
			<th></th>
		</tr>
	</thead>

	<tbody>
		<tr>
			<td>
<%# form_tag(specific_questions_url({ids: @ids}), method: :patch, id: :school_form) do %>
	<!-- DRY this up! -->	
<%= form_tag(specific_questions_url, method: :patch, id: :school_form) do %>
<% if !@ids.nil? %>
	<% @ids.each do |id| %>
		<%= hidden_field_tag "ids[]", id %>
	<% end %>
<% elsif !@entity_qid.nil? %>
		<%= hidden_field_tag "ids[]", @entity_qid[1..-1] %>
<% end %>
<%= hidden_field_tag :multiplex, 'schools' %>
  <div class="actions">
    <%= submit_tag "Go To School" %>
  </div>
<% end %>
			</td>
			<td>

<%= form_tag(specific_questions_url, method: :patch, id: :interest_form) do %>
<% if !@ids.nil? %>
	<% @ids.each do |id| %>
		<%= hidden_field_tag "ids[]", id %>
	<% end %>
<% elsif !@entity_qid.nil? %>
		<%= hidden_field_tag "ids[]", @entity_qid[1..-1] %>
<% end %>
<%= hidden_field_tag :multiplex, 'interests' %>
  <div class="actions">
    <%= submit_tag "Show An Interest" %>
  </div>
<% end %>
			</td>
			<td>

<%= form_tag(specific_questions_url, method: :patch, id: :subject_form) do %>
<% if !@ids.nil? %>
	<% @ids.each do |id| %>
		<%= hidden_field_tag "ids[]", id %>
	<% end %>
<% elsif !@entity_qid.nil? %>
		<%= hidden_field_tag "ids[]", @entity_qid[1..-1] %>
<% end %>
<%= hidden_field_tag :multiplex, 'subjects' %>
  <div class="actions">
    <%= submit_tag "Master A Subject" %>
  </div>
<% end %>
			</td>
		</tr>
	</tbody>
</table>

